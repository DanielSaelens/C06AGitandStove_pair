#When you make changes and want to push it, do this...

#If it's a new file, do this:
git add nameOfYourFile
#To figure out the name of the files that you changed, or new, use:
git status




git commit -m "Whatever comment"
git push -u origin main

#or just -> git push 

#If you need to sync stuff to yours, do:
git pull

